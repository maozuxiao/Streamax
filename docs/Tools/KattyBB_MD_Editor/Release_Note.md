# KattyBB Markdown Editor Release Notes

## Revision

| Version | Date       | Release Notes                                                |
| ------- | ---------- | ------------------------------------------------------------ |
| 0.99    | 2026-07-14 | Demo Version                                                 |
| 1.0     | 2026-07-15 | 1. 优化桌面宠物交互<br />2. 重构项目<br />3. 优化导出功能<br />4. 导出的HTML支持宽屏和目录功能<br />5. 修复渲染模块 |
| 1.1     | 2026-07-15 | 修复Github alert渲染问题                                     |
| 1.2     | 2026-07-15 | 修复TOC渲染                                                  |
| 1.3     | 2026-07-16 | 1. 优化导出HTML移动端适配<br />2. 优化导出PDF时首页上边距，以及存在横条的问题<br />3. 锚点适配与优化 |
| 1.4     | 2026-07-17 | 1. 移除TOC渲染显示的TOC title<br />2. 优化图片查看器UI       |
| 1.5     | 2026-07-18 | 优化图片查看器在手机端的使用场景                             |
| 1.6     | 2026-07-23 | 解决label 关联问题                                           |
| 1.7     | 2026-07-23 | 1. 新增复制粘贴富文本自动转为 Markdown（支持 Word/网页/Excel 等带格式内容，含 GFM 表格、任务列表、删除线，自动剥离 script/style 等脏内容）<br />2. 优化未打开文件时导出 HTML 与打印 PDF 以编辑器第一个标题命名（HTML title 与文件名前缀一致） |
| 1.8     | 2026-07-23 | 1. 优化导出 HTML 浏览器标签栏图标，使用 assets/Markdown.svg（绝对路径，保存到下载目录后图标仍有效） |
| 1.9     | 2026-08-04 | 1. 统一主题实时预览与导出 HTML 的渲染样式<br />2. 修复 everforest-light/everforest-dark 主题块引用（blockquote）内列表横向压缩、序号被截断的问题<br />3. 对齐 everforest 主题 H2 下边框与斜体（em）样式，使渲染窗口与导出 HTML 一致<br />4. 对齐 bloom 全系列主题导出 HTML 表格样式与编辑器实时渲染表格样式（仅表格，其他不变） |
| 1.9.1   | 2026-08-09 | 1. 优化撤销（Ctrl+Z）与重做（Ctrl+Y / Ctrl+Shift+Z）逻辑，修复撤销栈 off-by-one 缺陷导致修改后无法撤销的问题，并加入输入防抖结算以保证撤销点准确<br />2. 修复「撤销越过文档初始态」缺陷：在加载存档、打开本地文件、拖放文件、清空编辑器等外部程序化改写场景后重置撤销基准，避免一次撤销即把整篇已加载内容清空（表现为连续撤销时内容莫名全部消失） |
| 1.9.2   | 2026-08-19 | 1. 修复「清空」按钮缺陷：打开文件后即使编辑器已无内容，点击「清空」也会正确清除文件名（filename），不再残留上一文件的名称<br />2. 优化目录（TOC）超链接场景：标题含行内链接（如 [网址](url)）时，目录锚点 slug 已与正文标题 id 统一，且目录项内的真实链接被降级为不可点击文本 `<span class="toc-inner-link">`，避免 `<a>` 嵌套导致样式错乱与跳转失效（同步修复预览目录、悬浮目录与导出 HTML 目录）<br />3. 目录项增加 `title` 属性：当标题过长被截断时，鼠标悬停在目录项上可直接看到完整标题文本，不再只显示「目录」提示 |
| 1.9.3   | 2026-08-21 | 优化夜间模式代码块可读性：highlight.js 主题随明暗模式自动切换为 `github-dark`，避免夜间代码块文字与背景对比度过低导致看不清 |
| 1.10.0  | 2026-09-05 | 1. 新增 AI 辅助写作：在编辑器中选中文本后浮出指令条，可一键重写、润色、精简、扩写、纠错、中英互译，内置指令均可在设置中增删改<br />2. 新增 AI 侧边栏，支持围绕选中内容多轮追问（如「再正式一点」），生成过程中可随时停止<br />3. AI 结果以「原文 / 改写后」并排 diff 预览，确认后才写回文档，支持重试与放弃；写回后可按 Ctrl+Z 撤销。生成期间若文档被改动，会中止替换并提示重新选择<br />4. 新增配套浏览器扩展 Katty AI Bridge（独立仓库）：把 AI 请求下沉到扩展的 Service Worker，绕过 OpenAI / DeepSeek / 智谱 / Moonshot 等供应商的 CORS 限制；API Key 存于扩展内，不暴露给页面。未安装扩展时自动降级为直连，仅 OpenRouter 等开放 CORS 的供应商可用<br />5. AI 界面沿用主题变量，自动适配全部 10 套主题的明暗模式，并已接入中英双语 |
| 1.10.1  | 2026-09-05 | 1. 修复 AI 侧边栏设置图标与日夜主题切换图标相似易混淆的问题，改为标准齿轮造型<br />2. 修复选中文本后右键时，AI 指令条与格式右键菜单重叠的问题：右键的 mouseup 不再触发指令条弹出，且格式菜单弹出时指令条立即收起<br />3. 探测不到扩展时自动重试（每 2 秒，上限 15 次），页面重新可见时立即重新握手；设置面板新增「重新检测」按钮。此前的缺陷是 content script 注入晚于页面首次握手，导致扩展配置正常但页面一直显示「AI 不可用」 |
| 1.11.0  | 2026-09-06 | 1. 新增远程文档打开：编辑器支持 `?file=`（兼容 `?url=`）参数，从远端抓取 Markdown 载入阅读。来源限定为同源、`*.github.io` 与 `raw.githubusercontent.com` 且仅 https，避免沦为任意 URL 抓取代理<br />2. 支持直接传入 GitHub 文件页链接（`github.com/.../blob/...`），自动转换为 raw 地址后抓取（分支名含斜杠时转换不适用，请改用 raw 地址）<br />3. 新增 `&ro=1` 只读阅读态：编辑区只读且不写入本地草稿，远程文档不会覆盖原有内容；只读态下 AI 的「应用到文档」一并禁用<br />4. 远程文档中的相对图片与相对链接按文件所在目录补全为绝对地址，GitHub 根相对链接按仓库根补全，指向 md 的链接在编辑器内继续打开（本地文档行为不变）<br />5. 远程内容在渲染前做安全清洗，阻断 Markdown 中夹带的脚本注入；代码块里的示例文本不受影响 |
| 1.11.1  | 2026-09-06 | 1. 仅预览模式下笔记面板默认收起，不再自动展开挤占阅读区；用户手动开启过之后，后续切换视图会尊重其选择<br />2. 通过 `?file=` 打开远程文档时默认进入「仅预览」视图，阅读场景下不再显示编辑区。该设置仅本次会话生效，不会覆盖你保存的视图偏好 |
| 1.11.2  | 2026-09-06 | 1. 新增工具栏收起/展开按钮（Office 风格箭头，位于工具栏最右）：收起后仅保留主题切换与品牌标识，把手机上被工具栏占去的近四分之一屏还给正文<br />2. 窄屏（≤760px）或仅预览模式默认自动收起；手动切换后会记住你的选择，此后不再自动判定<br />3. 收起时格式工具栏一并隐藏，展开后自动恢复 |
| 1.11.3  | 2026-09-06 | 1. AI 侧边栏输入框旁新增「快捷指令」按钮（闪电图标），点开浮层即可选择内置或自定义指令；选中后**填入输入框而非直接发送**，可修改后再发（交互参照 mc-tool 的提示词面板）<br />2. 浮层底部提供「存为指令」（把当前输入框内容保存为自定义指令）与「管理」（打开设置中的指令管理）<br />3. 点击浮层外部或按 Esc 关闭；快捷指令与浮动指令条、设置中的自定义指令共用同一份数据，任一处增删改，三处即时同步 |
| 1.12.0  | 2026-09-14 | 1. 新增网页嵌入（iframe）支持：预览区与导出 HTML 可直接内嵌第三方内容（如 Sketchfab、YouTube、Bilibili、CodePen），此前 iframe 会在渲染前被整体删除<br />2. 嵌入安全策略：仅放行 `https:`（含协议相对 `//`）来源，自动移除 `on*` 事件属性并拦截 `javascript:`/`data:` 危险地址，非 http(s) 来源整体丢弃；`script`/`object`/`embed` 仍被拦截<br />3. 格式工具栏「插入」菜单新增「网页嵌入」：弹窗粘贴嵌入代码（支持多行）后自动提取 iframe、校验来源并插入正文光标处<br />4. 修复从网页复制含 iframe 的富文本粘贴进编辑器时嵌入被丢弃的问题<br />5. 预览区与导出 HTML 新增 iframe 响应式样式（宽度自适应、圆角、窄屏不溢出），导出 PDF 打印时整块不跨页切断 |
| 1.12.1  | 2026-09-15 | 修复 1.12.0 引入的初始化顺序缺陷：`IFRAME_*_ALLOWLIST` 曾写成文件末尾的模块级 `var`，而页面加载恢复存档后会**立刻**执行 `updatePreviewNow()`，此时该变量仍是 `undefined`；只要存档中含 `<iframe>` 就会抛 `TypeError: Cannot read properties of undefined (reading 'indexOf')`，脚本在初始化处中断——表现为**预览永久空白、输入无响应**（工具栏等后续初始化也未执行）<br />2. 现将白名单移入 `sanitizeIframeTag` 函数内部，彻底消除初始化顺序依赖；并给初始化渲染加 `try/catch` 兜底，避免单次渲染异常连带中断整个页面初始化 |
| 1.12.2  | 2026-09-15 | 1. AI 设置中的「直连设置」现在回填当前真正在用的连接：已装扩展时自动填入扩展当前供应商的 API 地址与模型，并在区块顶部显示来源提示；API Key 仍由扩展保管、页面不读取（输入框留空并给出占位说明），不破坏扩展的 Key 隔离设计<br />2. 自定义供应商支持免 API Key：本地 / 内网网关（如 codebuddy `http://127.0.0.1:8787/v1`）按地址自动判定为免 Key，扩展选项页新增「此端点需要 API Key」开关可手动覆盖；保存、测试连接与发起对话不再仅凭「是否填写了 API Key」拦截。需配合 Katty AI Bridge 扩展 v1.0.2 及以上 |
| 1.12.3  | 2026-09-15 | 1. 修复使用推理模型（如 hy4-preview）时 AI 对话必然超时的问题：推理模型会先输出一大段思考内容再给正文，实测一句简单润色就要 40 秒，而原先的超时是「总耗时 60 秒」一次性掐断。现改为**空闲超时**——只要流还在推进（正文或思考片段都算）就不断续期，另加 10 分钟绝对上限兜底<br />2. SDK 新增 `onReasoning` 回调，扩展通道可把模型的思考片段单独送给页面（不进答案）<br />3. 需配合 Katty AI Bridge 扩展 v1.0.4 及以上 |
| 1.12.4  | 2026-09-15 | 1. 修复 1.12.3 中思考进度写到了浮动指令条、在 AI 侧边栏里完全看不见的问题：现在思考过程直接显示在回复气泡**上方**，实时流式输出，并在标题上标注「思考中…（N 字）」<br />2. 正文开始输出后思考块自动折起为「已完成思考（N 字）· 点此展开」，可随时点开回看；非推理模型（没有思考片段）则整块不显示，不影响原有体验<br />3. 请求发出后立即显示思考块占位，不再出现「长时间只有一个光标、看不出是否卡死」的空白期 |
| 1.12.5  | 2026-09-15 | 1. 修复「编辑 + 预览」模式下点击悬浮目录项跳转失效的问题：跳转后预览一开始滚动就会触发同步滚动，把编辑器拽到映射位置，编辑器随即反向把预览拉回去，净位移只剩一点点（表现为「点一次只向下挪一小截」）<br />2. 现与脚注跳转保持一致——程序化跳转期间临时关闭同步滚动，滚动落定后自动恢复；预览区内的锚点链接点击同样修复<br />3. 新增 `scrollPreviewToElement()` 统一处理预览区内的程序化滚动，避免同类逻辑散落多处 |
| 1.12.6  | 2026-09-15 | 1. AI 思考过程改为**分段呈现**：按空行切块（原句分析 / 目标 / 候选版本…），段内保留换行，流式增量更新，不再是一整段糊在一起的纯文本<br />2. 目录跳转现在**预览与源码一起跳**：此前为了不被同步滚动拉回而临时关闭同步，导致编辑器停在原处；现在跳转时手动把编辑器带到对应标题行<br />3. 修复开启同步滚动时滚到文末**页面抖动**：两个方向的映射（按行 ↔ 按标题区间）不互逆，来回换算产生偏差，到底部时目标值越界被夹紧、偏差放大成持续拉扯。现改为写入前夹紧到合法区间、忽略亚像素差异，并在程序化滚动后短暂屏蔽该侧的 scroll 事件，杜绝回灌 |
| 1.12.7  | 2026-09-15 | 1. 目录跳转时源码侧改为**平滑滚动**，与预览侧观感一致，不再「瞬移」<br />2. 修复偶发「预览跳了、源码不跳」：原先靠预览区标题的 id 反查下标，遇到重名标题、或预览 id 与 TOC slug 在实体/行内链接处理上有差异时就会失配。现改为直接用编辑区标题文本算出的 slug 匹配（与 TOC 同一个 `slugifyTitle`），完全不依赖预览区 DOM<br />3. 支持定位重名标题：目录项携带「第几次出现」的序号，点击第二个乃至第 N 个同名标题也能跳到对应位置（此前 `getElementById` 只会拿到第一个） |
| 1.12.8  | 2026-09-15 | 1. 修复偶发「点目录只跳预览、源码不动，之后同步滚动彻底失效，只能刷新页面恢复」：程序化跳转原先直接把 `_syncScroll` 置 false，再「还原成跳转前的值」；一旦跳转发生在另一次跳转或**脚注跳转**的 1 秒暂停期内，取到的跳转前值就是 false，还原后同步滚动被永久关死——而刷新页面才会重新读 localStorage，所以刷新就好<br />2. 引入独立的暂停标记 `_syncPaused`：`_syncScroll` 现在只表示用户偏好，仅由工具栏的同步滚动按钮改变，任何跳转都不得改写它<br />3. 目录跳转是否带动源码一律以偏好为准，不再依赖「跳转前快照」；脚注跳转同样改用暂停标记 |
| 1.12.9  | 2026-09-15 | 1. 彻底修复「快速连续点击目录项时源码不跳」：恢复同步原本用固定 1 秒定时器，而平滑滚动是异步的、长文档里动画可能更久，提前恢复会让对侧的回灌把刚跳过去的一侧拽回去；连点两次时后一次还会重置计时器，两次动画叠在一起更晚才停。现改为**连续 6 帧（约 100ms）两侧 `scrollTop` 都不再变化**才恢复同步，另加 4 秒兜底<br />2. 编辑器侧滚动改为自绘 rAF 动画：原生 `behavior:'smooth'` 在快速连点时可能被上一次动画吞掉，表现为源码窗口干脆不动；自绘动画保证后一次请求必定取消并接管前一次<br />3. 目录项现在携带文档标题序号，源码定位优先按序号取值（与编辑区标题一一对应），标题文本/ID 匹配降为兜底，减少因渲染差异造成的失配 |
| 1.12.10 | 2026-09-15 | 1. 跳转落定判定适配卡顿页面：含 Sketchfab iframe / Mermaid 的文档单帧实测可达 658ms，原先 4 秒兜底会在动画真正停下前就恢复同步。现兜底放宽到 6 秒、静止判定由 6 帧降为 4 帧，并在**恢复同步后追加 350ms 静默期**，吞掉动画收尾的残余 scroll 事件<br />2. 落定时对编辑器做一次位置校正：万一动画期间被残余回灌拽偏，按期望终点复位<br />3. 修复**脚注跳转不同步源码**：此前只滚预览，开启同步滚动时源码停在原处；现在落定后按标题映射把编辑器也带过去 |
| 1.12.11 | 2026-09-15 | 1. 脚注跳转带动源码改为**平滑动画**：1.12.10 里落定后直接调用 `syncPreviewToEditor()` 赋值，源码是瞬移、观感生硬。现复用同一套映射算出目标位置，再交给 rAF 动画滚动过去<br />2. 把「预览位置 → 编辑器位置」的映射抽为 `editorPosFromPreview()`（纯计算、不写入），同步滚动与脚注跳转共用，避免两套算法各自漂移 |
| 1.12.12 | 2026-09-15 | 1. 修复目录跳转时**源码窗口滚动明显慢于预览窗口**：预览走原生 `behavior:'smooth'`（时长由浏览器按距离自行决定），编辑器走自绘动画，两套节奏天生对不齐。现两边共用**同一个 rAF 动画驱动**——同一条时间轴、同一时长、同一 easeOutCubic 缓动，滚动节奏完全一致<br />2. 预览目标与编辑器目标都改成纯计算函数（`previewTargetFor` / `editorTargetForHeading`），由跳转统一调度，不再各自触发滚动 |

